language: cpp

install:
  - sudo add-apt-repository --yes ppa:ubuntu-sdk-team/ppa
  - sudo apt-get update -qq
  - sudo apt-get install qt5-qmake qtbase5-dev libsqlite3-dev

script:
  - qmake -qt=qt5 -v
  - qmake -qt=qt5 IrcClient.pro
  - make